{
  "name": "@pie-ui/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "pie-ui": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --out-dir dist",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
